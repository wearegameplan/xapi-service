import { Request } from 'express';
declare const getHeader: (req: Request<import("express-serve-static-core").ParamsDictionary>, name: string, defaultValue?: any) => string;
export default getHeader;
